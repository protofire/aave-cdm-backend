import "mocha";
import { expect } from "chai";

describe("Autenticate", () => {
  it("Should autenticate successfully an already created user", async () => {});
  it("Should autenticate successfully an new user", async () => {});
});
